# Cargo configuration for redactor

[build]
# Optimize for size in release builds
# rustflags = ["-C", "link-arg=-s"]

[profile.release]
# Already defined in Cargo.toml, but documented here
# opt-level = 3
# lto = true
# codegen-units = 1
# strip = true
